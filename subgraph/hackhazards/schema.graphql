# schema.graphql
type PublicModel @entity(immutable: false) {
  id: ID! # Token ID
  owner: Bytes! # Owner address
  metadataURI: String! # IPFS URI
  modelName: String!
  role: String!
  visibility: String!
  timestamp: String!
  blockTimestamp: BigInt!
}